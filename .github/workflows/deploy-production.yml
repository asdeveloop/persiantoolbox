name: deploy-production

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      release_ref:
        description: 'Git ref (tag/branch/SHA) to deploy'
        required: true
        default: 'main'
      run_migrations:
        description: 'Run database migrations'
        required: true
        type: boolean
        default: true
      base_url:
        description: 'Base URL for post-deploy checks'
        required: true
        default: 'https://persiantoolbox.ir'
      post_report_strict:
        description: 'Fail workflow if post-deploy checks fail'
        required: true
        type: boolean
        default: true

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy-gate:
    uses: ./.github/workflows/deploy-gate.yml
    secrets: inherit

  deploy:
    needs: deploy-gate
    runs-on: ubuntu-latest
    timeout-minutes: 60
    environment: production
    env:
      APP_SLUG: persian-tools
      DEPLOY_BASE_DIR: /var/www/persian-tools
      SSH_OPTS: -o ServerAliveInterval=30 -o ServerAliveCountMax=10 -o TCPKeepAlive=yes
      RELEASE_REF: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.release_ref || github.sha }}
      RUN_MIGRATIONS: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.run_migrations || 'true' }}
      BASE_URL: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.base_url || 'https://persiantoolbox.ir' }}
      POST_REPORT_STRICT: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.post_report_strict || 'true' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ env.RELEASE_REF }}

      - name: Validate deploy secrets
        env:
          DEPLOY_SSH_KEY: ${{ secrets.DEPLOY_SSH_KEY }}
          DEPLOY_SSH_HOST: ${{ secrets.DEPLOY_SSH_HOST }}
          DEPLOY_SSH_PORT: ${{ secrets.DEPLOY_SSH_PORT }}
          DEPLOY_SSH_USER: ${{ secrets.DEPLOY_SSH_USER }}
          PRODUCTION_ENV_FILE_B64: ${{ secrets.PRODUCTION_ENV_FILE_B64 }}
        run: |
          set -euo pipefail
          required=(
            DEPLOY_SSH_KEY
            DEPLOY_SSH_HOST
            DEPLOY_SSH_PORT
            DEPLOY_SSH_USER
            PRODUCTION_ENV_FILE_B64
          )
          missing=()
          for key in "${required[@]}"; do
            if [[ -z "${!key:-}" ]]; then
              missing+=("${key}")
            fi
          done
          if (( ${#missing[@]} > 0 )); then
            echo "::error::Missing required deploy secrets: ${missing[*]}"
            exit 1
          fi
          if ! [[ "${DEPLOY_SSH_PORT}" =~ ^[0-9]+$ ]]; then
            echo "::error::DEPLOY_SSH_PORT must be numeric, got '${DEPLOY_SSH_PORT}'"
            exit 1
          fi

      - name: Compute release id
        id: meta
        run: echo "release_id=production-${GITHUB_RUN_ID}-$(git rev-parse --short=12 HEAD)" >> "$GITHUB_OUTPUT"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Enable pnpm
        run: |
          set -euo pipefail
          corepack enable
          for attempt in 1 2 3; do
            if corepack prepare pnpm@9.15.0 --activate; then
              pnpm -v
              exit 0
            fi
            if [[ "${attempt}" -lt 3 ]]; then
              sleep $((attempt * 5))
            fi
          done
          echo "::warning::corepack prepare failed after retries; falling back to npm global install."
          npm install --global pnpm@9.15.0
          pnpm -v

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Edge connectivity diagnostics (DNS/TLS/HTTP)
        run: |
          pnpm deploy:edge:check -- \
            --base-url="${BASE_URL}" \
            --extra-hosts="staging.persiantoolbox.ir" \
            --path="/api/health" \
            --dns-timeout-ms=2500 \
            --http-timeout-ms=7000 \
            --tls-timeout-ms=5000 \
            --strict=true

      - name: Install Playwright Chromium
        run: pnpm exec playwright install --with-deps chromium

      - name: Release gates
        run: |
          pnpm ci:quick
          pnpm ci:contracts
          pnpm deploy:readiness:run
          pnpm release:rc:run
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            pnpm release:launch:run
          else
            echo "[deploy-production] skipping release:launch:run for push-triggered auto deploy"
          fi

      - name: Build
        run: pnpm build

      - name: Real pre-deploy smoke test
        run: pnpm predeploy:smoke

      - name: Package release
        run: |
          RELEASE_ID="${{ steps.meta.outputs.release_id }}"
          ARCHIVE_PATH="/tmp/persian-tools-${RELEASE_ID}.tar.gz"
          tar -czf "${ARCHIVE_PATH}" \
            --exclude=.git \
            --exclude=.github \
            --exclude=node_modules \
            --exclude=coverage \
            --exclude=playwright-report \
            --exclude=test-results \
            --exclude=.next/cache \
            --exclude=docs/snapshots \
            .

      - name: Configure SSH
        run: |
          install -m 700 -d ~/.ssh
          echo "${{ secrets.DEPLOY_SSH_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p "${{ secrets.DEPLOY_SSH_PORT }}" -H "${{ secrets.DEPLOY_SSH_HOST }}" >> ~/.ssh/known_hosts

      - name: Remote preflight checks
        run: |
          ssh ${SSH_OPTS} -p "${{ secrets.DEPLOY_SSH_PORT }}" "${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}" "
            set -euo pipefail
            command -v node >/dev/null
            command -v pnpm >/dev/null
            command -v pm2 >/dev/null
            command -v rsync >/dev/null
            mkdir -p '${DEPLOY_BASE_DIR}/tmp' '${DEPLOY_BASE_DIR}/shared/env' '${DEPLOY_BASE_DIR}/shared/logs'
          "

      - name: Upload artifact and environment file
        run: |
          RELEASE_ID="${{ steps.meta.outputs.release_id }}"
          ARCHIVE_PATH="/tmp/persian-tools-${RELEASE_ID}.tar.gz"
          scp ${SSH_OPTS} -P "${{ secrets.DEPLOY_SSH_PORT }}" \
            "${ARCHIVE_PATH}" \
            "${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}:/tmp/persian-tools-${RELEASE_ID}.tar.gz"

          echo "${{ secrets.PRODUCTION_ENV_FILE_B64 }}" | base64 --decode > production.env

          scp ${SSH_OPTS} -P "${{ secrets.DEPLOY_SSH_PORT }}" \
            production.env \
            "${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}:/tmp/persian-tools-production.env"

      - name: Deploy on VPS
        id: deploy_vps
        run: |
          RELEASE_ID="${{ steps.meta.outputs.release_id }}"
          ssh ${SSH_OPTS} -p "${{ secrets.DEPLOY_SSH_PORT }}" "${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}" "
            set -euo pipefail
            BASE='${DEPLOY_BASE_DIR}'
            RELEASE='${RELEASE_ID}'
            TMP_DIR=\"\$BASE/tmp/\$RELEASE\"
            ARCHIVE_PATH=\"/tmp/persian-tools-\$RELEASE.tar.gz\"
            cleanup() {
              rm -rf \"\$TMP_DIR\" \"\$ARCHIVE_PATH\"
            }
            trap cleanup EXIT

            mkdir -p \"\$TMP_DIR\" \"\$BASE/shared/env\"
            mv /tmp/persian-tools-production.env \"\$BASE/shared/env/production.env\"
            chmod 600 \"\$BASE/shared/env/production.env\"

            tar -xzf \"\$ARCHIVE_PATH\" -C \"\$TMP_DIR\"
            chmod +x \"\$TMP_DIR/ops/deploy/deploy.sh\"

            \"\$TMP_DIR/ops/deploy/deploy.sh\" \
              --app-slug \"${APP_SLUG}\" \
              --env production \
              --base-dir \"\$BASE\" \
              --source-dir \"\$TMP_DIR\" \
              --release-id \"\$RELEASE\" \
              --keep-releases 5 \
              --run-migrations ${RUN_MIGRATIONS}
          "

      - name: Post-deploy runtime checks (GET/HEAD + locale + ASDEV)
        run: |
          set -euo pipefail
          for endpoint in /api/ready /api/health; do
            SAFE_ENDPOINT="${endpoint//\//-}"
            TARGET="${BASE_URL}${endpoint}"
            for i in {1..30}; do
              if curl -fsS --connect-timeout 5 --max-time 10 "${TARGET}" > "/tmp/persian${SAFE_ENDPOINT}.json"; then
                break
              fi
              sleep 2
            done
            GET_CODE="$(curl -sS --connect-timeout 5 --max-time 10 -o "/tmp/persian${SAFE_ENDPOINT}-get.json" -w '%{http_code}' "${TARGET}")"
            test "${GET_CODE}" = "200"
          done

          HEAD_CODE="$(curl -sSI --connect-timeout 5 --max-time 10 -o /tmp/persian-ready-head.txt -w '%{http_code}' -X HEAD "${BASE_URL}/api/ready")"
          test "${HEAD_CODE}" = "200"
          grep -q '"status":"ready"' /tmp/persian-api-ready-get.json
          grep -q '"status":"ok"' /tmp/persian-api-health-get.json
          curl -fsS "${BASE_URL}/asdev" | grep -q 'asdev_network'
          curl -fsS "${BASE_URL}/" | grep -Eq 'lang="fa"|dir="rtl"'

      - name: Generate production post-deploy report
        id: post_report
        timeout-minutes: 8
        run: |
          REPORT_NAME="post-deploy-production-${{ github.run_id }}.md"
          REMOTE_REPORT="/tmp/${REPORT_NAME}"
          REMOTE_STATUS="/tmp/${REPORT_NAME}.status"
          LOCAL_REPORT="docs/deployment/reports/${REPORT_NAME}"
          LOCAL_STATUS="docs/deployment/reports/${REPORT_NAME}.status"

          mkdir -p docs/deployment/reports

          ssh ${SSH_OPTS} -p "${{ secrets.DEPLOY_SSH_PORT }}" "${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}" "
            set -euo pipefail
            cd '${DEPLOY_BASE_DIR}/current/production'
            set -a
            source '${DEPLOY_BASE_DIR}/shared/env/production.env'
            set +a
            set +e
            node scripts/deploy/generate-post-deploy-report.mjs \
              --base-url='${BASE_URL}' \
              --environment=production \
              --git-ref='${RELEASE_REF}' \
              --workflow-run-url='https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}' \
              --deployer='${{ github.actor }}' \
              --output=\"${REMOTE_REPORT}\" \
              --strict='${POST_REPORT_STRICT}' \
              --migration-executed='${RUN_MIGRATIONS}' \
              --backup-path='${BACKUP_DIR}' \
              --backup-max-age-hours=36 \
              --retry-count=2 \
              --retry-delay-ms=400 \
              --timeout-ms=5000
            REPORT_EXIT=\$?
            set -e
            printf '%s' \"\$REPORT_EXIT\" > \"${REMOTE_STATUS}\"
            exit 0
          "

          scp ${SSH_OPTS} -P "${{ secrets.DEPLOY_SSH_PORT }}" \
            "${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}:${REMOTE_REPORT}" \
            "${LOCAL_REPORT}"

          scp ${SSH_OPTS} -P "${{ secrets.DEPLOY_SSH_PORT }}" \
            "${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}:${REMOTE_STATUS}" \
            "${LOCAL_STATUS}"

          ssh ${SSH_OPTS} -p "${{ secrets.DEPLOY_SSH_PORT }}" "${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}" "rm -f '${REMOTE_REPORT}' '${REMOTE_STATUS}'"

          REPORT_EXIT_CODE="$(cat "${LOCAL_STATUS}")"
          if [[ "${REPORT_EXIT_CODE}" != "0" ]]; then
            echo "post-deploy report failed with code ${REPORT_EXIT_CODE}" >&2
            exit "${REPORT_EXIT_CODE}"
          fi

      - name: Rollback production on post-deploy failure
        if: failure() && steps.deploy_vps.outcome == 'success'
        run: |
          ssh ${SSH_OPTS} -p "${{ secrets.DEPLOY_SSH_PORT }}" "${{ secrets.DEPLOY_SSH_USER }}@${{ secrets.DEPLOY_SSH_HOST }}" "
            set -euo pipefail
            bash '${DEPLOY_BASE_DIR}/current/production/ops/deploy/rollback.sh' --app-slug '${APP_SLUG}' --env production --base-dir '${DEPLOY_BASE_DIR}'
          "

      - name: Upload production post-deploy report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: production-post-deploy-report
          path: docs/deployment/reports/post-deploy-production-${{ github.run_id }}.md
          if-no-files-found: warn
